<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pratikcodes.codejava">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CodeJava">
        <activity android:name=".topics.SwitchActivity"></activity>
        <activity android:name=".topics.IfElseActivity" />
        <activity android:name=".topics.IOActivity" />
        <activity android:name=".topics.ArrayActivity" />
        <activity android:name=".topics.OperatorsActivity" />
        <activity android:name=".topics.DatatypesActivity" />
        <activity android:name=".topics.HelloworldActivity" />
        <activity android:name=".topics.SetupActivity" />
        <activity android:name=".topics.IntroActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>